name: neurotrace
description: A cyberpunk memory-tap game prototype
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"   # match Flutter stable (Dart 3)

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.16.0          # Dart 3 compatible

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
